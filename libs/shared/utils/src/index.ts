// Utilitaires transverses partagés par le portail et ses sous-applications.
// Lib volontairement présente dans les deux monorepos (même chemin, même alias
// @portail/shared-utils) pour qu'une sous-application puisse y faire référence
// quel que soit le portail dans lequel on la copie.
export {};
